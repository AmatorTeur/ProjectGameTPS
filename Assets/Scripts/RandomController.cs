using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;

public class RandomController : MonoBehaviour
{
/*    [SerializeField] public int _randomValue;
    public int RandomValue
    {
        get { return _randomValue; }
        set { _randomValue = value; }
    }
    private void Start()
    {
        _randomValue = Random.Range(1, 3);
        var variable = GetComponent<BehaviourManager>();
        if (variable != null)
        {
            variable.SetValue(_randomValue);
        }

    }*/
}